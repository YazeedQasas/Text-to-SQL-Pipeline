fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.27.0
qdrant-client==1.9.1
# Backs the repeated-question cache. hiredis is the optional C parser; it is not
# required, and the pure-Python fallback is used automatically without it.
redis[hiredis]==8.1.0
pymysql==1.1.1
cryptography==42.0.8
sqlparse==0.5.0
python-dotenv==1.0.1
pytest==8.2.2
# Test double for Redis, so the cache tests exercise the real client code paths
# without a running server.
fakeredis==2.37.0
